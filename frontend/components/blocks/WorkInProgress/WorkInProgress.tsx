import styled from 'styled-components';

const WorkInProgressWrapper = styled.div``;

const WorkInProgress = () => {
	return (
		<WorkInProgressWrapper>
			WorkInProgress
		</WorkInProgressWrapper>
	);
};

export default WorkInProgress;
